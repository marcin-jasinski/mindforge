package dev.mindforge.support;

// Static factory methods for domain objects used in tests.
// Method bodies are filled in as domain types are added in Phases 1+.
public final class TestFixtures {

    private TestFixtures() {}

    // Phase 1 additions: makeDocument(), makeKnowledgeBase(), makeUser(), makeDocumentArtifact()
}
